import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const Workflows = () => {
  return (
    <div>
      <Title level={2}>Workflows</Title>
      {/* Add your workflows management content here */}
    </div>
  );
};

export default Workflows; 