import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const Roles = () => {
  return (
    <div>
      <Title level={2}>Roles & Permissions</Title>
      {/* Add your roles and permissions management content here */}
    </div>
  );
};

export default Roles; 